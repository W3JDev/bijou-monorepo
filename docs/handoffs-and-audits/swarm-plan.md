# Swarm Execution Plan — Bijou AI Audit Fixes

Generated by the General agent (branch session `mvs_34f877de48e748529524d48da67c9323`) on 2026-07-20.

This document groups the 40 findings from `audit-report.md` into 6 parallel workstreams. Each workstream is independent of the others and can be picked up by a separate worker (human, Claude, or other agent). The General agent itself runs batches in parallel via the Read/Write/Edit tool surface.

---

## Workstream 1 — Security Hardening (CRITICAL — run first)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F1.1 | `api/send.js` is unauthenticated open proxy | `api/send.js` | Add `X-Internal-Token` check; restrict CORS to `mybijou.xyz` + `app.mybijou.xyz`; whitelist `to` field. **Or** delete the endpoint and route lead-notify through `api/leads.js` server-to-server. |
| F1.2 | `api/onboarding/signup.js` is unauthenticated open proxy | `api/onboarding/signup.js` | Same as F1.1. Phone format validation. |
| F1.3 | `leads.js` calls open proxy | `api/leads.js:339-358`, `OnboardingModal.tsx:316-340` | Move WhatsApp notify into `api/leads.js` itself (server-to-server to Fly.io with secret header). Remove client-side call. |
| F1.4 | Supabase `redirect` is open redirect | `backend/supabase/functions/redirect/index.ts` | Validate `destination_url` starts with `https://wa.me/` or `https://api.whatsapp.com/`; reject otherwise. |
| F1.5 | Supabase `create-link` is unauthenticated | `backend/supabase/functions/create-link/index.ts` | Require auth header. |
| F1.6 | RLS enabled, no policies | `backend/schema.sql:103-104` | Add `backend/rls-policies.sql` with INSERT/SELECT/UPDATE policies. |

## Workstream 2 — Headers & CSP (P1)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F2.1 | Incomplete security headers | `vercel.json` | Add CSP, HSTS, Referrer-Policy, Permissions-Policy, COOP, COEP. Remove `X-XSS-Protection`. |

## Workstream 3 — Marketing / Copy Reconciliation (P1)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F3.1 | DemoChat opening violates system rules | `components/DemoChat.tsx:20` | Replace "boleh tahu" with allowed phrase. |
| F3.2 | "4-Agent Pipeline" not implemented | `components/HowItWorks.tsx:100` | Replace headline with honest copy matching `Features.tsx:241`. |
| F3.3 | "30-day" vs "14-day" mismatch | `OnboardingModal.tsx:434,770`, `api/slide-deck.js:94`, `SlideDeckModal.tsx:85` | Standardize to "30-day money-back" wording. |
| F3.4 | Stale "5 languages" doc | `CLAUDE.md:30` | Change to 4. |

## Workstream 4 — Repo Hygiene (P1/P2)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F4.1 | 52.8 MB unreferenced brand PNGs | `public/brand/{1..12}.png` | Delete. |
| F4.2 | `dist/` committed at 53.75 MB | `dist/` | `git rm -r dist`. |
| F4.3 | Stale `AGENTS.md` line counts & arch | `AGENTS.md:88-92,113` | Update. |
| F4.4 | Stray `bijou-site-fixed.png` + `metadata.json` | repo root | Delete. |
| F4.5 | Dead `<script type="importmap">` | `index.html:265-280` | Delete block. |
| F4.6 | Dead `services/tools.ts` | `services/tools.ts` | Delete (and its import in any consumer — verify no consumers first). |
| F4.7 | PWA broken icons | `public/icons/icon-{192,512,180}.png` | Generate from `icon-base.svg`. |
| F4.8 | 51 unused i18n keys | `i18n.ts` | Delete dead keys (nav.*, pricing.{starter,professional,enterprise}.*, footer.*). |
| F4.9 | `calUsername` uses `process.env` | `components/CalBooking.tsx:18` | `import.meta.env` or hardcode. |
| F4.10 | Env-var typo `CUSTOME_API_ENDOINT` | `api/chat.js:96`, `.env` | Rename both. |
| F4.11 | `VITE_GEMINI_API_KEY` footgun | `.env` | Rename to `GEMINI_API_KEY`. |
| F4.12 | HowItWorks TRACE console spam | `components/HowItWorks.tsx:67-83` | Wrap in `import.meta.env.DEV`. |

## Workstream 5 — App Architecture Cleanup (P2)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F5.1 | `OnboardingModal.tsx` is 810-line god file | `components/OnboardingModal.tsx` | **SKIP** — risky, requires user-driven refactor. |
| F5.2 | No rate limiting on any endpoint | `api/*.js` | Add in-process LRU keyed on `x-forwarded-for`. |
| F5.3 | Centralize validators | `api/leads.js`, `api/slide-deck.js`, components | Extract `utils/validators.ts`. |
| F5.4 | Email regex too permissive | `api/leads.js:227` | Use stricter regex. |
| F5.5 | Lead-notify uses /api/send (F1.3 supersedes) | `OnboardingModal.tsx:316-340` | Move into combined /api/demo endpoint. |
| F5.6 | VoiceComingSoon shows success on error | `components/VoiceComingSoon.tsx:88-97` | Distinguish submitted vs optimistic. |

## Workstream 6 — Strict TS & Type Safety (P2)

| # | Finding | Files | Action |
|---|---------|-------|--------|
| F6.1 | No `strict: true` | `tsconfig.json` | Add strict + noUncheckedIndexedAccess. |
| F6.2 | 8 `any` catch-block usages | multiple | Change to `unknown` + narrow. |
| F6.3 | `api/*.js` not type-checked | `tsconfig.json:21-22` | JSDoc-annotate, or convert to `.ts`. |

---

## Execution Batches

The General agent executes these in **3 parallel batches** via the Read/Write/Edit tool surface:

### Batch 1 (Trivial, low-risk, ~15 parallel edits)
- F3.1, F3.2, F3.3, F3.4, F4.1, F4.2, F4.4, F4.5, F4.6, F4.9, F4.10, F4.12
- 12 file edits, 2 directory deletions

### Batch 2 (Security-critical, ~8 parallel edits)
- F1.1, F1.2, F1.3, F1.4, F1.5, F1.6, F2.1
- 6 file rewrites, 1 new migration file

### Batch 3 (Hygiene, ~6 parallel edits)
- F4.3, F4.8, F4.11, F6.1, F6.2, F6.3
- 5 file edits, 1 new env-rename plan

### Verification (single batch at end)
- Re-run `scripts/i18n_audit.py`, `scripts/i18n_unused.py`
- `npm audit`
- `npx tsc --noEmit`
- Re-verify with curl: `curl -I https://mybijou.xyz/.well-known/security.txt` (or local) for new headers
- Generate `delta-report.md`

### Skip
- F4.7 PWA icons (needs image generation — outside file-edit scope)
- F5.1 god-file split (user-driven refactor)
- F5.2 rate limiting (requires Redis/edge config — needs deployment discussion)
- F5.3 validators extraction (touches 5+ files, low priority)
- F5.4 email regex (covered by F5.3)
- F5.5 demo flow combine (touches 3 files, see F1.3 partial fix)
- F5.6 VoiceComingSoon error handling (cosmetic)
- F6.1-F6.3 strict TS (will surface many errors, requires broader audit)

---

## Notes for any spawned worker agent

- All paths in this document are workspace-relative.
- The repo is **not** a git repo on disk (`.git` not initialized); `git` commands will fail. Use file-system operations.
- `.env` and `.env.local` are gitignored but exist on disk. They are local secrets — do NOT print their contents in any report.
- `dist/` is committed to disk; the Vercel build produces its own fresh dist. After edits, the in-repo `dist/` will be stale — recommend `npm run build` after the fix batch to refresh.
- Run `python scripts/i18n_audit.py` before AND after i18n changes; output should stay at 238 keys per language.
- The `mavis` CLI exists for managing local agents but does NOT have a "spawn agent" command in this version. For real subagent dispatch, use the mavis team-plan machinery outside this session.
