{
  "functions_total": 25,
  "functions_with_search_path": 25,
  "functions_missing_search_path": [],
  "secdef_total": 19,
  "secdef_still_public": [],
  "secdef_clean": 19,
  "policies_total": 134,
  "policies_same_role_cmd": [],
  "indexes_total": 476,
  "indexes_dups_remaining": [],
  "indexes_partial_kept": [
    "blocked_numbers.idx_blocked_expires  CREATE INDEX idx_blocked_expires ON public.blocked_numbers USING btree (expires_at) WHERE (expires_at IS NOT NULL)",
    "blocked_numbers.idx_blocked_jid  CREATE INDEX idx_blocked_jid ON public.blocked_numbers USING btree (customer_jid) WHERE (is_active = true)",
    "campaigns.idx_campaigns_scheduled_start  CREATE INDEX idx_campaigns_scheduled_start ON public.campaigns USING btree (scheduled_start_at) WHERE (status = 'scheduled'::text)",
    "campaigns.idx_campaigns_tenant_status  CREATE INDEX idx_campaigns_tenant_status ON public.campaigns USING btree (tenant_id, status) WHERE (status = ANY (ARRAY['scheduled'::text, 'running'::text]))",
    "contacts.idx_contacts_campaign_config  CREATE INDEX idx_contacts_campaign_config ON public.contacts USING btree (campaign_config_id) WHERE (campaign_config_id IS NOT NULL)",
    "contacts.idx_contacts_do_not_contact  CREATE INDEX idx_contacts_do_not_contact ON public.contacts USING btree (tenant_id, do_not_contact) WHERE (do_not_contact = true)",
    "contacts.idx_contacts_industry_type  CREATE INDEX idx_contacts_industry_type ON public.contacts USING btree (tenant_id, industry_type) WHERE (industry_type IS NOT NULL)",
    "contacts.idx_contacts_interest_score  CREATE INDEX idx_contacts_interest_score ON public.contacts USING btree (tenant_id, interest_score DESC) WHERE (interest_score > 0)",
    "contacts.idx_contacts_outreach  CREATE INDEX idx_contacts_outreach ON public.contacts USING btree (tenant_id, last_outreach_at) WHERE (last_outreach_at IS NOT NULL)",
    "conversation_logs.idx_conversation_logs_failures  CREATE INDEX idx_conversation_logs_failures ON public.conversation_logs USING btree (tenant_id, success) WHERE (success = false)",
    "dashboard_views.idx_dashboard_views_pinned  CREATE INDEX idx_dashboard_views_pinned ON public.dashboard_views USING btree (pinned) WHERE (pinned = true)",
    "email_templates.idx_email_templates_tenant_type  CREATE INDEX idx_email_templates_tenant_type ON public.email_templates USING btree (tenant_id, template_type) WHERE (is_active = true)",
    "escalations.idx_escalations_agent_status  CREATE INDEX idx_escalations_agent_status ON public.escalations USING btree (assigned_agent_id, status) WHERE (assigned_agent_id IS NOT NULL)",
    "escalations.idx_escalations_sla  CREATE INDEX idx_escalations_sla ON public.escalations USING btree (sla_deadline) WHERE (status = ANY (ARRAY['pending'::text, 'claimed'::text]))",
    "escalations.idx_escalations_status_sla  CREATE INDEX idx_escalations_status_sla ON public.escalations USING btree (status, sla_deadline) WHERE ((status = ANY (ARRAY['pending'::text, 'in_progress'::text])) AND (sla_deadline IS NOT NULL))",
    "escalations.idx_escalations_timeout_candidates  CREATE INDEX idx_escalations_timeout_candidates ON public.escalations USING btree (tenant_id, status, escalation_triggered_at) WHERE (status = ANY (ARRAY['pending'::text, 'in_progress'::text]))",
    "escalations.idx_escalations_warning_candidates  CREATE INDEX idx_escalations_warning_candidates ON public.escalations USING btree (tenant_id, status, warning_sent, escalation_triggered_at) WHERE ((status = ANY (ARRAY['pending'::text, 'in_progress'::text])) AND (warning_sent = false))",
    "feedback.idx_feedback_follow_up  CREATE INDEX idx_feedback_follow_up ON public.feedback USING btree (follow_up_needed) WHERE (follow_up_needed = true)",
    "follow_ups.idx_follow_ups_pending  CREATE INDEX idx_follow_ups_pending ON public.follow_ups USING btree (status, scheduled_at) WHERE (status = 'pending'::text)",
    "follow_ups.idx_follow_ups_scheduled  CREATE INDEX idx_follow_ups_scheduled ON public.follow_ups USING btree (scheduled_at) WHERE (status = 'pending'::text)",
    "follow_ups.idx_follow_ups_unique_pending  CREATE UNIQUE INDEX idx_follow_ups_unique_pending ON public.follow_ups USING btree (tenant_id, chat_jid) WHERE (status = 'pending'::text)",
    "messages.idx_messages_conversation_key  CREATE INDEX idx_messages_conversation_key ON public.messages USING btree (conversation_key) WHERE (conversation_key IS NOT NULL)",
    "messages.idx_messages_device_jid  CREATE INDEX idx_messages_device_jid ON public.messages USING btree (device_jid) WHERE (device_jid IS NOT NULL)",
    "messages.idx_messages_system_event  CREATE INDEX idx_messages_system_event ON public.messages USING btree (tenant_id, is_system_event) WHERE (is_system_event = false)",
    "onboarding_sessions.idx_onboarding_token  CREATE INDEX idx_onboarding_token ON public.onboarding_sessions USING btree (token) WHERE (status <> 'completed'::text)",
    "outbound_queue.idx_outbound_queue_contact  CREATE INDEX idx_outbound_queue_contact ON public.outbound_queue USING btree (contact_id, campaign_id) WHERE (replied_at IS NOT NULL)",
    "outbound_queue.idx_outbound_queue_pending  CREATE INDEX idx_outbound_queue_pending ON public.outbound_queue USING btree (tenant_id, status, scheduled_at) WHERE (status = ANY (ARRAY['pending'::text, 'scheduled'::text]))",
    "outreach_campaign_configs.idx_occ_active  CREATE INDEX idx_occ_active ON public.outreach_campaign_configs USING btree (tenant_id, is_active) WHERE (is_active = true)",
    "personas.idx_personas_active  CREATE INDEX idx_personas_active ON public.personas USING btree (is_active) WHERE (is_active = true)",
    "scheduled_messages.idx_scheduled_messages_active_nextrun  CREATE INDEX idx_scheduled_messages_active_nextrun ON public.scheduled_messages USING btree (is_active, next_run_at) WHERE (is_active = true)",
    "scheduled_messages.idx_scheduled_messages_next_run  CREATE INDEX idx_scheduled_messages_next_run ON public.scheduled_messages USING btree (next_run_at) WHERE (is_active = true)",
    "scheduled_messages.idx_scheduled_messages_status_time  CREATE INDEX idx_scheduled_messages_status_time ON public.scheduled_messages USING btree (status, scheduled_time) WHERE (status = 'pending'::text)",
    "tenant_calendars.idx_tenant_calendars_tenant_id  CREATE INDEX idx_tenant_calendars_tenant_id ON public.tenant_calendars USING btree (tenant_id) WHERE (is_active = true)",
    "tenant_email_config.idx_tenant_email_config_tenant_id  CREATE INDEX idx_tenant_email_config_tenant_id ON public.tenant_email_config USING btree (tenant_id) WHERE (is_active = true)",
    "tenant_users_legacy.idx_tenant_users_main_contact  CREATE INDEX idx_tenant_users_main_contact ON public.tenant_users_legacy USING btree (tenant_id, is_main_contact) WHERE (is_main_contact = true)",
    "tenants.idx_tenants_auto_reply  CREATE INDEX idx_tenants_auto_reply ON public.tenants USING btree (auto_reply_enabled) WHERE (auto_reply_enabled = true)",
    "tenants.idx_tenants_handover_primary  CREATE INDEX idx_tenants_handover_primary ON public.tenants USING btree (handover_primary) WHERE (handover_primary IS NOT NULL)",
    "tenants.idx_tenants_testing_mode  CREATE INDEX idx_tenants_testing_mode ON public.tenants USING btree (testing_mode) WHERE (testing_mode = true)"
  ]
}